//LCD display driver
#import "6502.util"

//Memory Map
program_addr := 8000;
lcd_addr := 7f00

main : func () {
    init_stack();

    lcd_addr[0] = BACKGROUND_INSTRUCTION;
    init_lcd();

    run_lcd();
}

set_begin(program_addr, main);